@model IEnumerable<Category>

@if (Model.Count() > 0)
{
    <ul>
        @foreach (var category in Model)
        {
            <li>@category.Name</li>
        }
    </ul>
}
else
{
    <p>Brak kategorii</p>
}